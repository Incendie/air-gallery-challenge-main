import Gallery from "../Gallery";
import styles from "./Main.module.scss";

const Main = () => {
  return (
    <main className={styles.main}>
      <Gallery />
    </main>
  );
};

export default Main;
